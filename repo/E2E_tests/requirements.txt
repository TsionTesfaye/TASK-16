# Playwright pinned to 1.39.0 — the last release whose bundled driver
# ships Node.js 18.x (v18.18.0). Playwright 1.40+ switched the driver
# to Node 20. MUST match the image tag in E2E_tests/Dockerfile
# (mcr.microsoft.com/playwright/python:v1.39.0-jammy).
pytest==8.3.4
playwright==1.39.0
pytest-playwright==0.4.4
requests==2.32.3
